// Exercise 20

// Creating fruits array
let fruits = ["apple","banana","cherry"];

// Using Map to get the length of each fruit
let fruitLength = fruits.map((fruit)=> fruit +" = "+ fruit.length);

console.log(fruitLength);
